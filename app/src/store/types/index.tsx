
// user
export interface user {
    email: string
    organization: string
    name: string
}

// user profile
export interface userProfile {
    department: string
    isAdmin: boolean
}

// message
export interface messsage { 
    message: string
}